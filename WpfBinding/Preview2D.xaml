<UserControl x:Class="WpfBinding.Preview2D"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="330">
    <UserControl.Resources>
        <Style x:Key="Selected" TargetType="Line">
            <Setter Property="Stroke" Value="Red"/>
        </Style>
        <Style x:Key="Unselected" TargetType="Line">
            <Setter Property="Stroke" Value="Black" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Canvas Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" 
                Margin="10"
                x:Name="Canvas" MouseMove="Canvas_MouseMove" MouseDown="Canvas_MouseDown" MouseUp="Canvas_MouseUp">
            
        </Canvas>    
    </Grid>
</UserControl>
